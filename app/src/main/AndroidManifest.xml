<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rolling">

    <application
        android:allowBackup="true"
        android:icon="@drawable/rolling_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/rolling_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.LevelsActivity"
            android:label="@string/levels_activity"
            android:screenOrientation="portrait"/>
        <activity android:name=".activities.ShopActivity"
            android:label="@string/shop_activity"
            android:screenOrientation="portrait"/>
        <activity android:name=".activities.PurchasedActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".activities.GameActivity"
            android:label="@string/game_activity"
            android:screenOrientation="portrait"/>
        <activity android:name=".activities.FinishedLevelActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".activities.GameOverActivity"
            android:screenOrientation="portrait"/>
    </application>

</manifest>